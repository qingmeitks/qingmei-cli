# 青梅 (Qingmei CLI) 全工程实施方案与架构设计总览 (Implementation Plan)

本文档记录 **青梅 (Qingmei CLI)** 的完整架构设计、实施方案、核心模块划分及演进记录。后续所有新增功能与架构重构均在此文档中同步更新。

---

## 1. 项目定位与核心设计哲学

- **项目愿景**：打造一款极简、高响应、具备强自主推理能力且对用户工作区零污染的专业级 AI Agent 命令行开发助手。
- **核心原则**：
  1. **零参数极简启动**：终端仅需输入 `qingmei` 即可无缝进入交互式 REPL，首次启动自动引导配置。
  2. **零工作区文件污染**：所有配置文件、技能库、MCP 配置与全局用户提示词全部集中存放于全局主目录 `~/.qingmei/`，绝不在用户项目中生成垃圾配置文件。
  3. **自主 ReAct 推理闭环**：多步工具调用、思维链实时流式解析、智能循环检测与自我纠错。
  4. **单次运行纯内存生命周期**：操作历史与会话记忆仅在当前运行生命周期驻留内存，关闭终端即刻彻底清空，保障隐私与安全（支持按需手动导出）。
  5. **纵深防御与工作区沙箱**：4 级安全防御模式 + 工作区信任引导 + 底层绝对路径沙箱拦截。

---

## 2. 系统总体架构设计

```text
┌────────────────────────────────────────────────────────────────────────┐
│                        Qingmei CLI (全屏一体式 TUI)                     │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │ 顶部标题栏: Qingmei CLI v0.0.1                                    │  │
│  ├──────────────────────────────────────────────────────────────────┤  │
│  │ 中部历史视口 (Operation History Viewport - 内存驻留/平滑滚动/无滚动条)│  │
│  │  - 指令与结果紧密成对记录 (> command + result)                      │  │
│  │  - 悬浮式斜杠指令浮层 (Floating Suggestions Overlay - 零布局挤压)   │  │
│  ├──────────────────────────────────────────────────────────────────┤  │
│  │ 一体化输入舱: > 用户输入行                                        │  │
│  │ 底部状态栏: qingmei [mode] [model] [1M] [untrusted] | ~/relative │  │
│  └──────────────────────────────────────────────────────────────────┘  │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │
┌───────────────────────────────────▼────────────────────────────────────┐
│                    QingmeiAgent 核心自主推理层 (ReAct Loop)            │
│  ┌──────────────────┐  ┌───────────────────┐  ┌─────────────────────┐  │
│  │ ContextManager   │  │  SessionManager   │  │   SecurityGuard     │  │
│  │ 三层指令规则合成 │  │  单次运行纯内存   │  │   4-Tier 安全模式    │  │
│  │ 1M 上下文滑窗    │  │  支持 /session导出│  │   工作区信任/路径沙箱│  │
│  └──────────────────┘  └───────────────────┘  └─────────────────────┘  │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │
┌───────────────────────────────────▼────────────────────────────────────┐
│                       ToolDispatcher 工具分发引擎                       │
│  ┌─────────────────────────┐  ┌────────────────┐  ┌─────────────────┐  │
│  │ Built-in File/Shell 工具 │  │  MCP 扩展工具  │  │  Skill 自定义   │  │
│  │ read/write/edit/exec... │  │ (stdio / sse)  │  │  (SKILL.md 规范)│  │
│  └─────────────────────────┘  └────────────────┘  └─────────────────┘  │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │
┌───────────────────────────────────▼────────────────────────────────────┐
│                     LLMClient 多厂商模型适配层 (OpenAI 协议)           │
│  ┌───────────────────────────┐         ┌────────────────────────────┐  │
│  │ DeepSeek 官方预设         │         │ Google Gemini 官方预设     │  │
│  │ deepseek-v4-flash [1M]    │         │ gemini-3.7-flash [1M]      │  │
│  │ deepseek-v4-pro [1M]      │         │ gemini-3.5-flash-lite [1M] │  │
│  │                           │         │ gemini-3.1-pro-preview[1M] │  │
│  └───────────────────────────┘         └────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 3. 核心功能模块实施细则

### 3.1 全屏边框一体式 TUI 交互系统 (`src/cli/ui/prompt.ts`)
- **全屏铺满与呼吸感**：
  - 动态读取终端高度与宽度（`process.stdout.rows`, `columns`），自适应窗口缩放（Resize 即时重绘）。
  - 外框左右保留 2 字符的外边距 Padding 留白，界面居中通透。
- **上下布局分明**：
  - **顶部**：展示加大加粗的醒目高亮青色字符画标题（`QINGMEI CLI v0.0.1`）及使用提示，下方保留充足呼吸留白。

  - **中部历史视口 (Operation History Viewport)**：
    - 展示用户输入的指令 `> ...` 与其执行结果、思考链、工具反馈，两者紧密相连；不同轮次之间留空分隔。
    - **无滚动条平滑上下翻滚**：支持通过键盘 `↑` / `↓`（空输入状态下）、`PageUp` / `PageDown`、或鼠标滚轮上下滑动浏览完整历史，界面**不显示滚动条**。
    - **单次运行纯内存生命周期**：会话与操作历史仅在当前进程运行期间保存在 RAM 中，退出/关闭即彻底清空。
  - **底部一体化输入舱与双层信息栏**：
    - 输入舱与细线边框紧密集成。
    - **状态栏 (第一层)**：以**上下文实时使用量**作为开头标识（如 `[1.8k/1M (0.2%)]`，替换原 `qingmei` 文字），依次呈现运行模式 `[interactive]`、模型 `[deepseek-v4-flash]`、1M 规格 `[1M]`、推理强度 `[high]` 与未信任警告 `[untrusted]`，无冗余分隔符。
    - **路径栏 (第二层)**：当前工作区路径下移另起一行独立展示，以 `~` 相对路径精简呈现。

- **悬浮式指令浮层 (Floating Suggestions Overlay)**：

  - 输入 `/` 时，候选指令清单以**悬浮层**形式浮动覆盖在历史区域底部（输入舱正上方）。
  - **完全不挤压、不推移历史记录视口**，彻底杜绝输入提示时的界面抖动与高度跳跃。
  - 支持键盘 `↑` / `↓` 移动选择、`Enter` 回车一键选中执行、`Tab` 自动补全。

---

### 3.2 工作区信任机制与路径沙箱 (`src/core/security/`)
- **未信任工作区拦截引导**：
  - 当在尚未信任的目录启动青梅时，启动阶段主动弹出拦截引导，提供三种选项：
    1. **Yes, trust this workspace**：加入 `trustedWorkspaces`，启用完整工具。
    2. **No, run in Restricted Mode**：自动锁定为 `[readonly]` 严格只读模式，禁用文件修改与 Shell 命令。
    3. **Exit**：安全退出。
- **绝对路径沙箱防逃逸 (Path Containment)**：
  - 核心文件工具（`write_to_file`, `replace_file_content`, `read_file`）在执行前均进行 `isPathContained(targetPath, workspaceRoot)` 绝对路径沙箱校验。
  - 物理拦截跨越工作区根目录的目录遍历攻击（如 `../../etc/hosts`、`~/.ssh/id_rsa`），防止 Prompt 注入越界逃逸。
- **动态信任管理指令**：
  - `/trust [path]`：信任当前或指定工作区，持久化记录并解锁完整工具。
  - `/untrust [path]`：撤销信任并立即回退到受限只读保护模式。

---

### 3.3 4 级安全防御模式光谱 (`/mode`)
- **`[interactive]`** *(默认推荐)*：只读工具自动执行，写入文件及终端命令执行前强制进行人工确认。
- **`[auto]`**：全自主放行执行，无需任何交互确认中断。
- **`[readonly]`**：严格只读分析模式，物理拦截所有文件修改与 Shell 执行请求。
- **`[chat]`**：**纯对话模式**，完全禁用并剔除所有 Tool Schema，零 Token 开销，极速响应，杜绝模型幻觉调用工具。

---

### 3.4 精准模型预设与超大上下文感知 (`src/config/`)
- **官方精选预设 (Curated Presets)**：
  - **DeepSeek**：预设 `deepseek-v4-flash`（1M 上下文、支持推理与工具）与 `deepseek-v4-pro`（1M 上下文、支持工具）。
  - **Google Gemini**：预设 `gemini-3.7-flash`（1M 上下文、支持推理与工具）、`gemini-3.5-flash-lite`（1M 上下文）与 `gemini-3.1-pro-preview`（1M 上下文）。
  - **其他厂商**：暂未配置精选预设的厂商（OpenAI、Anthropic、GLM、Grok 等）已暂时注释隐藏，支持通过配置手动扩展。
- **零延迟启动**：取消每次进入 REPL 时的后台远程模型探测，启动零延迟、界面零扰动。
- **1M 上下文动态预算**：精准识别 1M+ 超长上下文模型，状态栏与模型列表实时呈现 `[1M]` 规格徽章。

---

### 3.5 三层指令约束体系 (Layered Instructions)
在每次 Agent 推理循环中，系统严格按照以下层级合成 System Prompt：
1. **核心 Agent System Prompt**：角色定义、ReAct 思考与工具使用规则、安全模式限制。
2. **全局用户指令 (`~/.qingmei/QINGMEI.md`)**：跨工程全局生效，适合沉淀个人编码风格与通用代码审查准则。
3. **项目根目录约束 (`./AGENTS.md`)**：当前工作空间生效，适合配置项目架构规范、测试命令（`npm test`）与团队开发 SOP。
4. **技能扩展指令 (`SKILL.md`)**：当通过 `/skills` 激活技能时按需动态注入。

---

### 3.7 推理/思考强度调节体系 (`/effort`)
- **解耦设计**：`/model` 决定模型底座，`/effort` 决定思考深度与 Token 预算。
- **4 档推理强度**：
  - **`off`**：关闭思考机制（零思考延迟，快速纯文本直出）。
  - **`low`**：轻量级思考（~1k Token 预算，极速低延迟响应）。
  - **`medium`** *(默认推荐)*：平衡思考（~8k Token 预算，兼顾深度与速度）。
  - **`high`**：深度推理（~32k Token 预算，攻坚复杂算法、大跨度重构与疑难 Bug）。
- **无图标精简状态栏徽章**：针对具备推理能力的模型，状态栏直接展示极简纯文字标签 `[high]` / `[medium]` / `[low]` / `[off]`，不带多余前缀与 emoji 图标，界面极简干练；对于传统非推理模型自动隐去。

---

### 3.8 上下文使用量动态计算与 Token 计量引擎 (`src/core/context.ts`)
- **高精度混合分词计量**：内置针对中英文与代码混合场景的 Token 估算算法（`estimateTokenCount`），精准覆盖 System Prompt、三层指令约束、思维链 (Reasoning Content) 与工具调用参数 (Tool Calls Schema)。
- **动态规格与百分比呈现**：自适应换算为 `k` / `M` 规范单位（如 `1.8k/1M (0.2%)` 或 `14.2k/200k (7.1%)`），每次会话交互与模式切换即时联动刷新。

---

### 3.9 内置智能缓存对齐与自适应上下文压缩技术 (`src/core/context/compressor.ts`)
- **Layer 1: 服务商 KV Cache 友好型前缀对齐 (Prefix Alignment)**：
  - 将角色指令、`~/.qingmei/QINGMEI.md`、`./AGENTS.md` 与激活的 `SKILL.md` 严格置于最头部作为静态前缀，最大化命中 DeepSeek / Gemini / Claude 等底层官方 KV Cache（最高降低 90% 重复计费与 5x 首字加速）。
- **Layer 2: 历史超大工具输出微裁剪 (Micro-Folding)**：
  - 对远期轮次中过长（如 > 1500 字符）的只读文件读取与搜索结果自动进行头尾保留与中间折叠，杜绝中间冗余日志膨胀。
### 3.10 会话全生命周期管理与断点续接体系 (`src/core/session/storage.ts`)
- **保持启动极简秒开**：不增加任何 CLI 启动参数，所有会话管理完全在 REPL 终端内通过 `/session` 统一调度。
- **静默自动保存 (Auto-Draft)**：每轮对话执行完毕后，系统自动在全局 `~/.qingmei/sessions/<ws-hash>/` 中静默写入/更新当前会话快照，意外中断或退出零数据丢失。
- **显式命名归档 (Named Save)**：通过 `/session -s [name]` 为关键开发节点打上自定义语义标签。
- **100% 零代码库污染**：所有会话 JSON 状态文件按工程绝对路径哈希隔离保存于全局用户主目录，代码仓库保持极致纯净。
- **完整子指令与短选项**：
  - `/session list` / `/session -l`：浏览工作区历史会话。
  - `/session save [name]` / `/session -s [name]`：命名保存当前会话快照。
  - `/session resume [id]` / `/session -r [id]`：恢复历史会话（缺省 id 时唤起交互式选择菜单）。
  - `/session delete [id]` / `/session -d [id]`：删除历史会话（缺省 id 时唤起交互式选择菜单）。
  - `/session export [id] [path]` / `/session -e [id] [path]`：导出 Markdown 审查报告（默认导出至全局 `~/.qingmei/export/` 保持代码库纯净）。

---

## 4. 指令集参考表 (Slash Commands)

| 指令 | 说明 | 交互形式 |
| :--- | :--- | :--- |
| `@<filepath>` | 智能文件/目录补全并在首轮即时预读注入模型上下文 | 输入 `@` 触发文件浮层选择器 |
| `!<command>` | 快速 Shell 直通执行（本地子进程运行，零 Token 消耗） | 命令行输入即时执行与耗时回显 |
| `/mode [mode]` | 切换运行安全模式（`interactive`, `auto`, `readonly`, `chat`） | 支持参数直切或正中央选择器 |
| `/model [name]` | 切换 AI 厂商与模型 | 支持分组选择与 API Key 动态录入 |
| `/effort [level]` | 调整思考/推理强度（`off`, `low`, `medium`, `high`） | 支持参数直切或正中央选择器 |
| `/skills` | 查看已安装的技能列表，并可按需开启或禁用 | 多选弹窗 (Multiselect) |
| `/mcp` | 查看已连接的 MCP 服务器健康状态与工具清单 | 列表输出 |
| `/trust [path]` | 信任当前或指定工作区，解锁完整写/执行工具与自定义技能 | 即时生效并写入配置 |
| `/untrust [path]` | 撤销工作区信任，立即进入受限只读保护模式 | 即时生效并锁定受限模式 |
| `/compact` | 立即压缩与优化当前会话上下文（折叠长日志并提炼关键摘要） | 即时生效并刷新状态栏 |
| `/session [subcmd]` | 会话全生命周期管理：`-l` (列表), `-s` (保存), `-r` (恢复), `-d` (删除), `-e` (导出) | 支持参数直切与正中央选择器 |
| `/clear` | 清空终端屏幕并重置当前会话的上下文记忆 | 即时清空 |
| `/config` | 查看当前生效的全局配置（支持 `/config edit` 一键唤起系统编辑器） | 终端输出 / 编辑器唤起 |
| `/help` | 显示完整的指令帮助清单 | 终端输出 |
| `/exit` 或 `/quit` | 退出青梅终端并断开所有后台 MCP 进程 | 安全退出 |





---

## 5. 项目目录结构与模块映射

```text
qingmei/
├── bin/
│   └── qingmei.ts                  # CLI 入口
├── src/
│   ├── cli/
│   │   ├── commands/               # CLI 子命令 (config 等)
│   │   ├── ui/
│   │   │   ├── prompt.ts           # 全屏一体式 TUI、历史视口、悬浮提示层
│   │   │   ├── renderer.ts         # 终端输出与流式高亮渲染
│   │   │   └── theme.ts            # 主题配色与模式徽章
│   │   └── repl.ts                 # REPL 主循环、信任向导与斜杠指令路由
│   ├── config/
│   │   ├── defaults.ts             # 官方预设 (DeepSeek/Gemini)、全局路径
│   │   ├── loader.ts               # 配置加载、合并、校验与持久化
│   │   └── types.ts                # TypeScript 配置与模型元数据类型定义
│   ├── core/
│   │   ├── llm/                    # 多厂商 LLM 客户端与流式解析器
│   │   ├── security/               # 工作区信任引擎与路径沙箱 (trust.ts)
│   │   ├── agent.ts                # QingmeiAgent 核心推理引擎
│   │   ├── context.ts              # 上下文滑窗与三层指令合成引擎
│   │   ├── dispatcher.ts           # 工具分发与执行度量器
│   │   ├── security.ts             # 4 级安全守卫检查器 (SecurityGuard)
│   │   └── session.ts              # 纯内存会话管理器与 Markdown 导出
│   ├── mcp/
│   │   ├── client.ts               # MCP 客户端核心协议实现
│   │   └── manager.ts              # MCP 多服务器生命周期管理
│   ├── skills/
│   │   ├── loader.ts               # SKILL.md 规范解析器
│   │   └── manager.ts              # 技能注册与动态注入管理
│   └── tools/
│       ├── builtin/                # 内置核心工具 (read, write, edit, exec 等)
│       ├── registry.ts             # 工具注册表与安全分级
│       └── types.ts                # 工具接口与执行上下文定义
├── test/                           # 完整单元测试套件 (8 个测试套件，28 项测试)
├── README.md                       # 英文官方文档
├── README.zh-CN.md                 # 中文官方文档
├── IMPLEMENTATION_PLAN.zh-CN.md    # 中文实施方案与架构设计总览 (本文件)
└── package.json
```

---

## 6. 测试与质量保障标准

- **测试框架**：基于 [Vitest](https://vitest.dev/) 进行高覆盖率单元测试与集成测试。
- **验证命令**：`npm run build && npm test`
- **质量门禁标准**：
  1. 编译构建零 Warning、零 Error。
  2. 100% 单元测试通过率（覆盖配置、模型、提示词、工具、安全守卫、上下文、MCP、工作区信任与路径沙箱）。
  3. 终端窗口缩放（Resize）自适应重绘不破框。
  4. 严格杜绝在用户工作区生成任何未授权文件。
