# 青袂 (Qingmei CLI) 全工程实施方案与架构设计总览 (Implementation Plan)

本文档记录 **青袂 (Qingmei CLI)** 的完整架构设计、实施方案、核心模块划分及版本演进记录。

---

## 1. 项目定位与核心设计哲学

- **项目愿景**：打造一款极简、高响应、具备强自主推理能力且对用户工作区零污染的专业级 AI Agent 命令行开发助手。
- **核心原则**：
  1. **零参数极简启动**：终端仅需输入 `qingmei` 即可无缝进入交互式 REPL，首次启动自动引导配置。
  2. **零工作区文件污染**：所有配置文件、技能库、MCP 配置与全局用户提示词全部集中存放于全局主目录 `~/.qingmei/`，绝不在用户项目中生成垃圾配置文件。
  3. **自主 ReAct 推理闭环**：多步工具调用、思维链实时流式解析、智能循环检测与自我纠错。
  4. **多会话并发运行与无缝切入**：单 Workspace 下支持多个平铺会话（Multi-Session），支持后台异步执行与 `Tab` 快捷轮巡切换。
  5. **纯粹文字极简 UI (Zero Icons)**：全界面 100% 杜绝 Emoji/图标装饰，采用纯文本状态标签 (`[running]`, `[ready]`, `[waiting]`, `[done]`, `[error]`)。
  6. **纵深防御与工作区沙箱**：4 级安全防御模式 + 工作区信任引导 + 底层绝对路径沙箱拦截。

---

## 2. 系统总体架构设计

```text
┌────────────────────────────────────────────────────────────────────────┐
│                        Qingmei CLI (全屏一体式 TUI)                     │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │ 顶部标题栏: 字符画标志 + CLI v0.1.1                                │  │
│  ├──────────────────────────────────────────────────────────────────┤  │
│  │ 中部历史视口 (Operation History Viewport)                        │  │
│  │  - 当前激活会话的历史记录渲染                                    │  │
│  │  - 原生居中模态弹窗 (Native Centered Modals)                      │  │
│  │  - 悬浮式斜杠指令浮层 (Floating Suggestions Overlay - 零布局挤压)   │  │
│  ├──────────────────────────────────────────────────────────────────┤  │
│  │ 一体化输入舱: > 用户输入行                                        │  │
│  │ 底部状态栏 Line 1: [1.2k/1M] [interactive] [gemini-3.7-flash] ... │  │
│  │ 底部状态栏 Line 2: 自适应会话标签/折叠状态 + 工作区路径            │  │
│  └──────────────────────────────────────────────────────────────────┘  │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │
┌───────────────────────────────────▼────────────────────────────────────┐
│                    MultiSessionPool 多会话并发控制池                   │
│  ┌─────────────────────────┐  ┌─────────────────────────────────────┐  │
│  │ SessionInstance #1      │  │ SessionInstance #2 (后台并发运行中) │  │
│  │ [ready] - 独立会话记忆  │  │ [running] - 异步非阻塞执行          │  │
│  │ 独立历史行 / 独立上下文 │  │ 独立 AbortController 任务中断信号   │  │
│  └─────────────────────────┘  └─────────────────────────────────────┘  │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │
┌───────────────────────────────────▼────────────────────────────────────┐
│                    QingmeiAgent 核心自主推理层 (ReAct Loop)            │
│  ┌──────────────────┐  ┌───────────────────┐  ┌─────────────────────┐  │
│  │ ContextManager   │  │  SessionStorage   │  │   SecurityGuard     │  │
│  │ 三层指令规则合成 │  │  平铺快照持久化   │  │   4-Tier 安全模式    │  │
│  │ 1M 上下文滑窗    │  │  ~/.qingmei/sess/ │  │   工作区信任/路径沙箱│  │
│  └──────────────────┘  └───────────────────┘  └─────────────────────┘  │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │
┌───────────────────────────────────▼────────────────────────────────────┐
│                       ToolDispatcher 工具分发引擎                       │
│  ┌─────────────────────────┐  ┌────────────────┐  ┌─────────────────┐  │
│  │ Built-in File/Shell 工具 │  │  MCP 扩展工具  │  │  Skill 自定义   │  │
│  │ read/write/edit/exec... │  │ (stdio / sse)  │  │  (SKILL.md 规范)│  │
│  └─────────────────────────┘  └────────────────┘  └─────────────────┘  │
└───────────────────────────────────┬────────────────────────────────────┘
                                    │
┌───────────────────────────────────▼────────────────────────────────────┐
│                     LLMClient 多厂商模型适配层 (OpenAI 协议)           │
│  ┌──────────────────┐  ┌───────────────────────┐  ┌─────────────────┐  │
│  │ DeepSeek 预设    │  │ Google Gemini 预设    │  │ OpenAI 预设     │  │
│  │ deepseek-v4-flash│  │ gemini-3.5-flash      │  │ gpt-5.6-sol     │  │
│  │ deepseek-v4-pro  │  │ gemini-3.5-flash-lite │  │ gpt-5.6-terra   │  │
│  │                  │  │ gemini-3.6-flash      │  │ gpt-5.6-luna    │  │
│  │                  │  │ gemini-3.7-flash      │  │ gpt-5.5         │  │
│  │                  │  │ gemini-3.1-pro-preview│  │ gpt-5.4-mini    │  │
│  └──────────────────┘  └───────────────────────┘  └─────────────────┘  │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 3. 多会话 (Multi-Session) 架构细则 (v0.1.0)

### 3.1 平铺式存储与内存模型
- **平铺结构**：一个 Workspace 对应多个 flat Sessions，不存在 session 嵌套。
- **存储路径**：`~/.qingmei/sessions/<ws-hash>/<sessionId>.json`。
- **内存生命周期**：每个 `SessionInstance` 拥有独立上下文管理器、独立历史显示缓冲区、独立运行状态以及独立的 `AbortController`。

### 3.2 纯全单词顶层指令集 (Zero Short-Flags)
取消所有单字母简写参数（如 `-s`, `-r`, `-d`, `-e`, `-l`），统一为语义明确的顶层指令：

| 顶层全单词直接指令 | 标准指令 | 功能说明 | 示例 |
| :--- | :--- | :--- | :--- |
| **`/new [name]`** | `/session new [name]` | 新建并切入新会话 | `/new 模块重构` |
| **`/switch [id]`** | `/session switch [id]` | 切换到指定会话（空参弹出选择器，支持空行按 `Tab` 轮巡） | `/switch 1` |
| **`/sessions`** | `/session list` | 查看所有活动会话及磁盘存档列表 | `/sessions` |
| **`/rename [name]`** | `/session rename [name]` | 重命名当前活动会话 | `/rename API设计` |
| **`/close [id]`** | `/session close [id]` | 关闭并释放指定会话内存 | `/close 2` |
| **`/save [name]`** | `/session save [name]` | 命名保存当前会话快照至磁盘 | `/save checkpoint-1` |
| **`/resume [id]`** | `/session resume [id]` | 从磁盘恢复/唤醒历史会话 | `/resume sess_123` |
| **`/delete [id]`** | `/session delete [id]` | 从磁盘删除指定会话快照 | `/delete sess_123` |
| **`/export [id]`** | `/session export [id]` | 导出当前会话为 Markdown 报告 | `/export` |

### 3.3 底部双层状态栏与超宽自适应
- **Line 1 (指标栏)**：`[1.2k/1M] [interactive] [gemini-3.7-flash] [1M] [high]`
- **Line 2 (自适应会话栏 + 路径)**：
  - **标准模式 (1~4 个会话)**：
    `[#1 (running)] [#2]* [#3] | ~/Workspace/aispace/qingmei`
  - **紧凑模式 (4~6 个会话)**：
    `[#1 (running)] [#2] [#3] [#4]* [#5] | ~/Workspace/aispace/qingmei`
  - **超宽折叠聚合模式 (场景 3: >6 个或超出终端宽度)**：
    `#5* [running] (8 sessions: 2 running, 6 ready) | ~/Workspace/aispace/qingmei`

### 3.4 退出拦截守卫 (Exit Guard)
当输入 `/quit` 或 `/exit` 时：
- 若所有会话均处于空闲状态，自动持久化当前活动会话并安全退出。
- 若存在后台正在执行中的会话（`[running]` 或 `[waiting]`），主动弹出居中警告模态框，告知当前正在运行的会话列表，由用户确认是否强制终止并退出。

---

## 4. API Key 动态管理与智能降级架构 (v0.1.0)

### 4.1 独立密钥管理中心 (`/key` 与 `/apikey`)
- **交互式管理**：直接输入 `/key`，列出所有支持厂商的脱敏 Key 预览（如 `DeepSeek (sk-****3f9a)`、`Google Gemini (AIza****782b)`、`OpenAI [unconfigured]`）。
- **二级操作菜单**：
  - `[ Update API Key ]`：修改/配置该厂商的 Key。
  - `[ Test Connection / Auth ]`：快速发起连通性探活，测试 Key 是否有效。
  - `[ Remove / Unbind API Key ]`：清除该厂商 Key。
- **参数化快捷直达**：
  - `/key deepseek sk-xxxx`：一行命令静默校验并更新。
  - `/key rm deepseek` 或 `/key delete deepseek`：一行命令解绑。

### 4.2 连通性探测自测与错误诊断建议 (Verification & Suggestions)
- 更新/输入新 Key 时，系统使用轻量探活探测凭证有效性（耗时 < 800ms，0 额度损耗）。
- **验证失败时**，弹出居中诊断模态框：
  - **错误诊断**：呈现具体原因（如 `Error: 401 Unauthorized - Incorrect API key provided`）。
  - **排查建议**：检查 Key 是否过期/撤回、检查末尾是否混入空格、检查账户余额及代理连通性。
  - **三向操作选项**：`[ Re-enter API Key ]`（重输） / `[ Save Anyway ]`（强存） / `[ Cancel ]`（放弃）。

### 4.3 运行时客户端热重载与模型选用流转 (Runtime Hot-Reloading & Model Flow)
- **更新当前活动厂商 Key**：即刻热重载内存中的 `agent.llmClient` 与多会话并发池，不中断会话。
- **更新非当前厂商 Key**：直接弹出直观选择菜单：
  - `> [ Switch to <Provider> (<defaultModel>) ]`（直接切换至该厂商的默认模型，一键生效）
  - `  [ Select specific model for <Provider> ]`（立即进入该厂商的模型列表挑选特定模型）
  - `  [ Keep current model (<activeModel>) ]`（保存 Key 但不切换当前活动模型）
- **消除冲突**：无论是通过 `/key` 还是 `/model` 输入 Key，均收敛至上述统一步骤，不再出现“先确认默认模型又重复弹出模型列表”的冲突逻辑。

### 4.4 `/model` 流程中的就地维护增强
- 输入 `/model` 选择已配置厂商时，弹出选项：
  - `> [ Select Model ]`（默认高亮，按回车直接进入模型选择，对正常切模型 0 干扰）。
  - `  [ Update API Key ]`：就地修改 Key。
  - `  [ Remove API Key ]`：就地解绑 Key。

### 4.5 删除 Key 智能降级策略 (Smart Fallback)
- **删除非当前厂商的 Key**：当前运行模型与会话不受任何影响。
- **删除当前活动厂商的 Key**：
  - 若系统中还有其他可用厂商（如 Gemini），弹出选择器推荐**一键平滑降级切换**至该模型，并即时刷新底部状态栏。
  - 若所有厂商均无有效 Key，主动拦截并引导重新输入。

---

## 5. TUI 交互体验与稳定性演进架构 (v0.1.1)

### 5.1 视口历史滚动浏览与按键分工
- **`Shift + Up` / `Shift + Down`**：平滑上下滚动浏览终端主视口中的对话历史。
- **滚动状态高亮指示**：当视口偏离底部历史时，底部浮现醒目指示徽章：
  `▲ Scrolled back N lines [X-Y/Total] (Shift+Down to return)`
- **`Up` / `Down` 按键严格专职化**：严格限定为输入舱历史命令回溯与候选项切换，不再直接接管视口滑动，消除操作冲突。

### 5.2 生成中断与非阻塞中止 (Stream Interruption)
- **快捷中断**：在模型流式思考/回答生成过程中，按 `Esc` 或 `Ctrl + C` 立即触发 `agent.abort()`。
- **级联信号分发**：底层通过 `AbortController` 级联终止 LLM 的 HTTP/SSE 网络流与正在执行的工具调用子进程。
- **会话持久化与状态机收敛**：中断后完整保留已生成的片段，并在历史末尾追加 `⏹ Generation stopped by user.`，将当前会话安全恢复至 `ready` 状态。
- **响应空消息清洗**：对 Assistant 响应进行防御性清洗，杜绝因 `content` 为空引发的 `400 Bad Request` 异常。

### 5.3 悬浮半透明覆盖层与快照批量删除 (Floating Overlay Modal)
- **原生悬浮模态框**：在主视口正中央动态计算并覆盖渲染悬浮对话框，无需清空终端或重新创建全屏进程。
- **会话快照批量删除 (`/delete`)**：
  - 支持多选模态框（Space 切换选择、Enter 确认批量删除）。
  - 支持根据 ID、名称、前缀、后缀快速匹配删除。
  - 支持 `/delete all` 一键清空所有磁盘快照。

### 5.4 退出指令规范化 (Command-Only Exit)
- **禁用 `Ctrl + C` / `Ctrl + D` 退出**：在输入框为空时按下 `Ctrl + C` 或 `Ctrl + D` 不再直接退出程序，仅给出友好提示 `(To exit, use /exit)`。
- **退出通道收敛**：退出 CLI 必须且仅能通过 `/exit` 或 `/quit` 显式指令执行，避免开发过程中误触快捷键导致丢失现场。

### 5.5 运行时元认知与系统上下文注入 (Runtime Meta-Context)
- **元认知注入**：在 System Prompt 的元数据区域直接注入当前 Provider、当前模型 ID/规格、上下文窗口尺寸、可用斜杠命令列表及已配置可选模型列表。
- **杜绝工作区盲目扫描**：明确规范 Agent 在回答自身模型与 CLI 运行状态问题时，直接根据系统提示词上下文回答，严禁调用工具扫描用户工作区源代码或执行 Shell 命令。

### 5.6 终端缩放自适应与残影擦除 (Resize Artifact Elimination)
- **全屏原子擦除**：在 `renderBox` 输出流末尾统一追加 `\x1b[J`（Erase in Display：清除光标后所有残余行），杜绝终端窗口尺寸缩小时底部遗留旧状态栏残影。
- **`onResize` 缓冲区重置**：终端尺寸变化事件触发时，主动执行 `\x1b[2J\x1b[3J\x1b[H` 擦除历史缓冲区并即刻按新行列重绘。

### 5.7 单元测试沙箱隔离与 Key 持久化同步
- **测试环境临时目录隔离**：通过 `process.env.QINGMEI_CONFIG_PATH` 将单元测试隔离于系统临时沙箱目录（`os.tmpdir()`），确保 `npm test` 绝不污染用户真实配置文件 `~/.qingmei/config.json`。
- **配置持久化即时同步**：通过 `/key` 交互录入或更新的 API Key 即时同步至 `process.env`，确保运行时即刻生效且磁盘文件永久保存。


