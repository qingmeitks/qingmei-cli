# 更新日志 (Changelog)

<p align="center">
  <a href="CHANGELOG.md">English</a> | <b>简体中文</b>
</p>

---

本文档记录 **青袂 (Qingmei CLI)** 的所有重要版本演进与变更历史。

版本号严格遵循 [语义化版本 2.0.0 (Semantic Versioning)](https://semver.org/lang/zh-CN/) 规范。

---

## [0.1.1] - 2026-08-28

### 新增特性 (Added)
- **视口历史内容滚动浏览**：
  - `Shift + Up` / `Shift + Down`：平滑上下滚动浏览终端主视口对话历史。
  - 滚动至历史区域时，视口底部显示清晰的高亮指示徽章 (`▲ Scrolled back N lines [X-Y/Total] (Shift+Down to return)`)。
- **生成中断与停止回答**：
  - 在模型思考与流式回答生成过程中，按 `Esc` 或 `Ctrl+C` 可随时安全终止生成，不退出 CLI。
- **悬浮模态框与会话快照批量删除**：
  - 全新居中悬浮半透明覆盖层（Overlay Modal），提供更好的确认与多选体验。
  - `/delete` 会话快照批量删除支持（支持多选模态框、ID/前缀/后缀匹配以及 `/delete all`）。
- **运行时元认知与模型信息注入**：
  - 将当前 Provider、当前模型详情与已配置可选模型列表注入 System Prompt，禁止盲目扫描工作区代码查找 CLI 模型配置。

### 变更优化 (Changed)
- **快捷键职责清晰划分**：
  - `Up` / `Down` 方向键严格专属用于输入舱的历史命令回溯与候选项切换。
  - 退出程序严格限定为通过指令 `/exit`（或 `/quit`）执行，避免 `Ctrl+C` / `Ctrl+D` 误触退出。
- **API Key 持久化与测试沙箱隔离**：
  - 单元测试全面使用临时沙箱隔离，杜绝测试数据覆盖 `~/.qingmei/config.json` 真实密钥的问题。
  - 确保文件配置与环境变量的即时同步。

### 缺陷修复 (Fixed)
- **终端缩放残影与异常字符消除**：
  - 追加 `\x1b[J` 清屏指令并在窗口尺寸变化（`resize`）时清空屏幕缓冲区，彻底解决终端缩小时底部状态栏重复显示的问题。
  - 移除鼠标移动追踪转义码，杜绝输入舱自动录入 `34;61;30M...` 等坐标碎片。
- **LLM 流式结束事件与消息清洗**：
  - 补全 `LLMClient.chatStream` 流式末尾的 `done` 事件分发，确保工具调用结果与回答稳定写入视口。
  - 清洗空 Assistant 消息，杜绝 `400 Bad Request` 异常。

---

## [0.1.0] - 2026-08-22

### 新增特性 (Added)
- **多会话并发执行与切换引擎 (`MultiSessionPool`)**：
  - 单 Workspace 下支持多个平铺平行会话，各会话内存状态完全隔离。
  - 支持后台非阻塞异步执行：后台会话执行复杂任务的同时，可无缝切入前台会话进行对话与交互。
  - 每个会话独享独立的上下文管理器、终端历史缓冲区、未提交草稿与 `AbortController` 任务中断信号。
  - 输入框为空时按 `Tab` 键可一键快速轮巡切换活动会话。
- **顶层全单词直接指令集 (Zero Short-Flags)**：
  - `/new [name]`：新建并切入新会话。
  - `/switch [id]`：切换活动会话（无参调用时弹出居中选择模态框）。
  - `/sessions`：查看当前内存中所有活跃会话及磁盘存档快照。
  - `/rename [name]`：重命名当前活动会话。
  - `/close [id]`：关闭并释放指定会话内存（自动安全平跳至相邻会话）。
  - `/save [name]`：保存当前会话快照至 `~/.qingmei/sessions/<ws-hash>/`。
  - `/resume [id]`：从磁盘恢复/唤醒历史会话。
  - `/delete [id]`：从磁盘删除指定会话快照（支持 ID、名称、前缀、后缀及 `/delete all`）。
  - `/export [id]`：导出会话对话记录为 Markdown 报告。
- **自适应双层状态栏**：
  - **Line 1 (指标栏)**：Token 实时使用量 (`[1.8k/1M (<0.1%)]`)、运行模式 `[interactive]`、模型 `[gemini-3.7-flash]`、`[1M]` 规格徽章、推理强度 `[high]` 与未信任警告。
  - **Line 2 (会话栏 + 路径)**：智能适配三种宽度模式：
    - *标准模式 (1~4 个会话)*：`[#1 (running)] [#2]* [#3] | ~/path`
    - *紧凑模式 (4~6 个会话)*：`[#1 (running)] [#2] [#3] [#4]* [#5] | ~/path`
    - *超宽折叠聚合模式 (>6 个会话或窄屏)*：`#5* [running] (8 sessions: 2 running, 6 ready) | ~/path`
- **API Key 动态管理与智能降级 (`/key` 与 `/model`)**：
  - 独立 `/key` 密钥管理中心：支持查看所有已支持厂商的脱敏 Key（如 `sk-****3f9a`）、轻量连通性探活与错误诊断排查建议。
  - 参数化快速更新（`/key deepseek sk-xxxx`）与一键删除解绑（`/key rm deepseek`）。
  - **运行时客户端热重载**：修改当前活动厂商的 Key 时，即刻热重载 `LLMClient`，无需重启进程，且 100% 保持会话上下文与历史记忆。
  - **`/model` 流程就地管理**：在选择已配置厂商时，支持直接选择模型、修改 Key 或删除 Key。
  - **删除 Key 智能降级**：删除当前活动厂商的 Key 时，自动探测并引导平滑切换至其他已配置厂商模型。
- **扩展 Google Gemini 预设模型库**：
  - 完整预设并支持 `gemini-3.5-flash`、`gemini-3.5-flash-lite`、`gemini-3.6-flash`、`gemini-3.7-flash` 与 `gemini-3.1-pro-preview`（全量 1M+ 上下文）。
- **新增 OpenAI GPT-5.x 官方预设模型**：
  - 正式开放 OpenAI 厂商，预设 `gpt-5.6-sol`、`gpt-5.6-terra`、`gpt-5.6-luna`、`gpt-5.5`（全量 1M 上下文、支持推理与工具）与 `gpt-5.4-mini`（1M 上下文、支持工具）。
- **退出拦截守卫 (Exit Guard)**：
  - 输入 `/quit` 或 `/exit` 时，若检测到后台仍有 `[running]` 或 `[waiting]` 状态的会话，主动弹出居中警告模态框，防止误关闭未完成任务。

### 体验优化 (Changed)
- **纯文字极简 UI 重构 (Zero Icons)**：
  - 全界面 100% 剔除所有 Emoji 与装饰性图标，统一采用高密度纯文字状态徽章 (`[running]`, `[ready]`, `[waiting]`, `[done]`, `[error]`)。
- **纯粹视口清屏 (`/clear`)**：
  - `/clear` 回归纯粹清屏职责：仅清空终端视觉历史，**完整保留当前会话的上下文记忆与 Token 状态**。
- **输入流单管道归一化**：
  - 重构 `TuiPrompt` 底层输入流水线，彻底消除中文输入法中英切换与剪贴板粘贴导致的字符重复输入问题。

---

## [0.0.1] - 2026-08-21

### 新增特性 (Added)
- **青袂 (Qingmei CLI) 首次开源发布**。
- **自主 ReAct Agent 推理引擎**：
  - 多步工具调用与思维链 (Thinking/Reasoning) 实时流式解析。
  - 推理强度分级控制：`off`, `low`, `medium`, `high`。
  - 内置死循环检测与自我纠错机制。
- **全屏边框一体式 TUI**：
  - 全屏铺满终端宽高，外框舒适留白，顶部呈现 ASCII 字符画标题。
  - 平滑滚动历史视口（支持鼠标滚轮与键盘翻页，无冗余滚动条）。
  - 悬浮式斜杠指令补全浮层（零布局挤压）。
- **4 级安全防御模式**：
  - `[interactive]`：读工具自动放行，写/执行工具需人工确认。
  - `[auto]`：全自主执行模式。
  - `[readonly]`：严格只读分析模式，物理拦截写文件与命令执行。
  - `[chat]`：纯对话模式，移除全部 Tool Schema，零 Token 开销极速响应。
- **工作区信任与路径沙箱**：
  - 首次打开未信任工作区时主动拦截引导。
  - 路径沙箱防逃逸机制，物理拦截穿越工作区根目录的写操作。
- **Model Context Protocol (MCP) 深度集成**：
  - 原生支持基于 `stdio` 进程沙箱与 `sse` 远程端点的 MCP 协议工具。
  - 自动命名空间隔离 (`mcp__<server>__<tool>`)。
- **Skill 技能扩展体系**：
  - 标准 `SKILL.md` 解析器与全局 `~/.qingmei/skills/` 技能仓库。
- **1M+ 超大上下文感知**：
  - DeepSeek 与 Google Gemini 官方预设模型深度适配。
- **零工作区污染**：
  - 100% 配置文件、会话快照与缓存数据集中存放于全局 `~/.qingmei/`。
